/**
 * DEFER
 * 1. Downloads in parallel with HTML DOM elements.
 * 2. Executes in the order placed.
 * 3. Executes before the DOM Content Loaded event.
 * 
 * ASYNC
 * 1. Runs in "load-first" order.
 * 2. Totally independent of the DOM and other scripts.
 * 3. Can run before and after the DOMContentLoaded event.
 */